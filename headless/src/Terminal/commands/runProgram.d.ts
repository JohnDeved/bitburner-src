import { BaseServer } from "../../Server/BaseServer";
import { ProgramFilePath } from "../../Paths/ProgramFilePath";
export declare function runProgram(path: ProgramFilePath, args: (string | number | boolean)[], server: BaseServer): void;
