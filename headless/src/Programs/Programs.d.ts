import { Program } from "./Program";
import { CompletedProgramName } from "@enums";
export declare function getEffectiveHackingLevelRequirement(level: number): number;
export declare const Programs: Record<CompletedProgramName, Program>;
