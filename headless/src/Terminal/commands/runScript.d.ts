import { BaseServer } from "../../Server/BaseServer";
import { ScriptFilePath } from "../../Paths/ScriptFilePath";
export declare function runScript(scriptPath: ScriptFilePath, commandArgs: (string | number | boolean)[], server: BaseServer): void;
